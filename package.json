{
  "name": "pt-tracker",
  "homepage": "https://<you>.github.io/pt-tracker/",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}

